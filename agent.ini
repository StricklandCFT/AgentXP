[agent]
; Logstash HTTP input endpoint
logstash_url=http://127.0.0.1:8080/

; Optional: add static fields to every event
static_host=XP-AGENT
static_env=lab

; Batch settings
batch_max=100
batch_interval_ms=2000

; Raw dump file path for reverse-engineering
raw_dump_path=procmon-raw.bin
